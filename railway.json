{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn studentproject.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
