{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "web: python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn studentproject.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
